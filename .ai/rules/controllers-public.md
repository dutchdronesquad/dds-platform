---
paths:
  - app/Http/Controllers/Public/ArticleController.php
---

# Controllers Public

## Preview saved draft articles without publishing
Article previews render only persisted content through an authenticated, policy-authorized route. Mark previews visibly, set noindex and nofollow, open them in a separate tab, disable preview while the form is dirty, and never change status or published_at.
